apiVersion: v1
kind: Service
metadata:
  name: todo-frontend-service
  namespace: todo-app-prod
  labels:
    app: todo-frontend
    environment: production
spec:
  selector:
    app: todo-frontend
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP